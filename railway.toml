[build]
builder = "dockerfile"
dockerfilePath = "job-search-engine/Dockerfile"

[deploy]
startCommand = "node dist/server/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
